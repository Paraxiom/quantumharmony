# QuantumHarmony Deployment Modes - Choose Your Flavor

**Interactive Decision Tree for Node Operators**

---

## 🌍 Step 1: Choose Your Market

```
                    ┌─────────────────────────┐
                    │   YOUR DEPLOYMENT       │
                    │   LOCATION?             │
                    └────────┬────────────────┘
                             │
                ┏────────────┴────────────┓
                ▼                         ▼
      ┌─────────────────┐      ┌─────────────────┐
      │  WESTERN MARKET │      │  ASIAN MARKET   │
      │  (Nokia/US/EU)  │      │  (CN/JP/KR/SG)  │
      └────────┬────────┘      └────────┬────────┘
               │                         │
               ▼                         ▼
      ┌─────────────────┐      ┌─────────────────┐
      │  NOKIA MODE     │      │  ASIA MODE      │
      │  Pure PQC       │      │  PQC + QKD      │
      │  --nokia        │      │  --enable-qkd   │
      └─────────────────┘      └─────────────────┘
```

---

## 🔧 Step 2: Device Onboarding (Local Setup)

### Nokia Mode Setup

```bash
# Terminal 1: Start KIRQ Hub (Device Aggregator)
cd ~/kirq-hub
./kirq-hub start \
  --devices toshiba,crypto4a,idquantique \
  --port 8001 \
  --mode nokia

# Devices auto-discovered via USB/Serial:
# ✓ Toshiba QKD   @ /dev/ttyUSB0 (QBER: 11%)
# ✓ Crypto4A HSM  @ /dev/hsm0    (QBER: 5%)
# ✓ IdQuantique   @ 192.168.0.4  (QBER: 8%)

# Terminal 2: Start Validator Node
./target/release/quantumharmony-node \
  --validator \
  --name "Nokia-Validator-Alice" \
  --base-path /data/validator \
  --chain nokia-mainnet \
  --nokia \
  --kirq-endpoint http://localhost:8001 \
  --bootnodes /dns/boot.quantumharmony.io/tcp/30333/p2p/12D3KooWABC...
```

### Asia Mode Setup (with QKD Enhancement)

```bash
# Terminal 1: Start KIRQ Hub with QKD
cd ~/kirq-hub
./kirq-hub start \
  --devices toshiba,crypto4a,idquantique \
  --port 8001 \
  --mode asia \
  --qkd-enable \
  --qkd-peer 192.168.0.5:3001  # Remote validator's QKD

# Terminal 2: Start Validator with QKD
./target/release/quantumharmony-node \
  --validator \
  --name "Asia-Validator-Alice" \
  --base-path /data/validator \
  --chain asia-mainnet \
  --enable-qkd \
  --kirq-endpoint http://localhost:8001 \
  --qkd-transport \
  --bootnodes /dns/boot.quantumharmony.asia/tcp/30333/p2p/12D3KooWXYZ...
```

---

## 🌐 Step 3: Node Connectivity (P2P Network)

### Network Stack Visualization

```
┌──────────────────────────────────────────────────────────────┐
│  APPLICATION LAYER                                           │
├──────────────────────────────────────────────────────────────┤
│  Entropy Gossip    CoherenceVotes    BlockAnnounce          │
│  /qh/entropy/1     /qh/coherence/1   /substrate/1           │
└────────────────────────┬─────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────┐
│  ENCRYPTION LAYER (Choose One)                               │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  NOKIA MODE:                    ASIA MODE:                   │
│  ┌────────────────────┐        ┌────────────────────┐       │
│  │ Falcon1024         │        │ Falcon1024 + QKD   │       │
│  │ + AES-256-GCM      │        │ + AES-256-GCM      │       │
│  └────────────────────┘        │ + QKD Key XOR      │       │
│                                 └────────────────────┘       │
└────────────────────────┬─────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────┐
│  TRANSPORT LAYER (QSSH - Quantum-Safe SSH)                   │
├──────────────────────────────────────────────────────────────┤
│  - Post-quantum KEX (CRYSTALS-Kyber)                         │
│  - Post-quantum signatures (Dilithium/Falcon)                │
│  - Forward secrecy with quantum-resistant DH                 │
└────────────────────────┬─────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────┐
│  NETWORK LAYER (libp2p + NAT Traversal)                      │
├──────────────────────────────────────────────────────────────┤
│  ┌────────────┐ ┌────────────┐ ┌────────────┐              │
│  │   Direct   │ │    STUN    │ │   Relay    │              │
│  │ Connection │ │  Hole Punch│ │  Fallback  │              │
│  └────────────┘ └────────────┘ └────────────┘              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔀 Step 4: NAT Traversal (Decentralized)

### Decentralized NAT Architecture

```
SCENARIO 1: Both Nodes Behind NAT
─────────────────────────────────

    ┌──────────────┐                        ┌──────────────┐
    │ Validator A  │                        │ Validator B  │
    │ (Alice)      │                        │ (Bob)        │
    │ NAT: 🏠      │                        │ NAT: 🏠      │
    └──────┬───────┘                        └──────┬───────┘
           │                                        │
           │ 1. Request relay                       │
           ▼                                        ▼
    ┌──────────────────────────────────────────────────────┐
    │         SUBSTRATE RELAY NODES (DHT)                  │
    │  (Run by validators, rotating selection)             │
    ├──────────────────────────────────────────────────────┤
    │  Relay-1 (Charlie)   Relay-2 (Dave)   Relay-3 (Eve) │
    │  /ip4/1.2.3.4        /ip4/5.6.7.8      /ip4/9.0.1.2 │
    └──────────────────────────────────────────────────────┘
           │                                        │
           │ 2. Circuit established                 │
           │                                        │
           └──────────► QSSH Tunnel ◄──────────────┘
                        (Encrypted)


SCENARIO 2: One Node Public IP (Direct Connection)
──────────────────────────────────────────────────

    ┌──────────────┐                        ┌──────────────┐
    │ Validator A  │                        │ Validator B  │
    │ (Alice)      │  ──────────────────►   │ (Bob)        │
    │ NAT: 🏠      │   Direct QSSH          │ Public IP ✓  │
    └──────────────┘                        └──────────────┘
                      No relay needed!


SCENARIO 3: STUN Hole Punching (Preferred)
──────────────────────────────────────────

    ┌──────────────┐                        ┌──────────────┐
    │ Validator A  │                        │ Validator B  │
    │ NAT: 🏠      │                        │ NAT: 🏠      │
    └──────┬───────┘                        └──────┬───────┘
           │                                        │
           │ 1. Discover external IP/port          │
           ▼                                        ▼
    ┌──────────────────────────────────────────────────────┐
    │      DISTRIBUTED STUN SERVERS (DHT)                  │
    │  stun-1.qh.io    stun-2.qh.io    stun-3.qh.io       │
    └──────────────────────────────────────────────────────┘
           │                                        │
           │ 2. Simultaneous open                   │
           │    (UDP hole punching)                 │
           │                                        │
           └──────────► Direct Connection ◄────────┘
                        (QSSH encrypted)
```

---

## 🎯 Step 5: Entropy Distribution Flow (Multi-Node)

### Full Multi-Validator Flow

```
BLOCK #5 VRF ELECTION - Alice Wins
──────────────────────────────────

┌─────────────────────────────────────────────────────────────┐
│ Step 1: Leader Collects Device Entropy                     │
└─────────────────────────────────────────────────────────────┘

  Validator Alice (Leader):
  ┌────────────────┐
  │ 🎲 VRF Winner  │
  └────────┬───────┘
           │
           │ HTTP GET /entropy/{device}
           ▼
  ┌────────────────────────────┐
  │ KIRQ Hub (localhost:8001)  │
  ├────────────────────────────┤
  │ • Toshiba:   Share 1/3     │
  │ • Crypto4A:  Share 2/3     │
  │ • IdQuantique: Share 3/3   │
  └────────────────────────────┘
           │
           │ Shamir Reconstruction (K=2)
           ▼
  ┌────────────────────────────┐
  │ Combined Entropy (30 bytes)│
  └────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│ Step 2: Encrypt for Each Validator                         │
└─────────────────────────────────────────────────────────────┘

  Alice encrypts for each validator:

  For Bob:                    For Charlie:
  ┌──────────────────┐       ┌──────────────────┐
  │ AES-256 Key      │       │ AES-256 Key      │
  │ (random 32 bytes)│       │ (random 32 bytes)│
  └────────┬─────────┘       └────────┬─────────┘
           │                           │
           │ Encrypt entropy           │
           ▼                           ▼
  ┌──────────────────┐       ┌──────────────────┐
  │ Ciphertext       │       │ Ciphertext       │
  └────────┬─────────┘       └────────┬─────────┘
           │                           │
           │ Encrypt AES key           │
           ▼ with Bob's Falcon PK      ▼ with Charlie's Falcon PK
  ┌──────────────────┐       ┌──────────────────┐
  │ EntropyMessage   │       │ EntropyMessage   │
  │ (523 bytes)      │       │ (523 bytes)      │
  └──────────────────┘       └──────────────────┘


┌─────────────────────────────────────────────────────────────┐
│ Step 3: Gossip Distribution                                │
└─────────────────────────────────────────────────────────────┘

  Alice ──┬─► Bob      (via QSSH + libp2p)
          │
          ├─► Charlie  (via QSSH + libp2p)
          │
          ├─► Dave     (via relay if NAT)
          │
          └─► Eve      (via direct connection)


┌─────────────────────────────────────────────────────────────┐
│ Step 4: Validators Decrypt & Vote                          │
└─────────────────────────────────────────────────────────────┘

  Bob receives:
  ┌──────────────────────┐
  │ EntropyMessage       │
  │ recipient_hash=Bob's │
  └──────────┬───────────┘
             │
             │ 1. Verify: recipient_pubkey_hash matches
             │ 2. Verify: Alice's Falcon signature valid
             │ 3. Decrypt: AES key with Bob's Falcon privkey
             │ 4. Decrypt: entropy with AES key
             ▼
  ┌──────────────────────┐
  │ Combined Entropy ✓   │
  │ (30 bytes)           │
  └──────────┬───────────┘
             │
             │ 5. Vote: Accept/Reject
             ▼
  ┌──────────────────────┐
  │ EntropyVote          │
  │ signed by Bob        │
  └──────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│ Step 5: Byzantine Consensus (2/3 threshold)                │
└─────────────────────────────────────────────────────────────┘

  Votes collected:
  ┌──────────────────────────────────┐
  │ ✓ Alice:   Accept (leader)       │
  │ ✓ Bob:     Accept                │
  │ ✓ Charlie: Accept                │
  │ ✓ Dave:    Accept                │
  │ ✗ Eve:     Reject (bad QBER)     │
  ├──────────────────────────────────┤
  │ Result: 4/5 = 80% > 66.67% ✓     │
  └──────────────────────────────────┘
           │
           │ Consensus reached!
           ▼
  ┌──────────────────────────────────┐
  │ Entropy finalized on-chain       │
  │ Block #5 sealed with quantum RNG │
  └──────────────────────────────────┘
```

---

## 🔐 Security Properties Comparison

| Feature | Nokia Mode | Asia Mode |
|---------|------------|-----------|
| **Device Entropy** | K=2 of M=3 Shamir | K=2 of M=3 Shamir |
| **Transport Encryption** | QSSH (PQC only) | QSSH (PQC + QKD) |
| **Message Encryption** | Falcon1024 + AES-256-GCM | Falcon1024 + AES-256-GCM + QKD XOR |
| **Signature** | Falcon1024 | Falcon1024 |
| **Forward Secrecy** | ✓ (ephemeral keys) | ✓ (ephemeral + QKD) |
| **Quantum Resistance** | ✓ (NIST PQC) | ✓✓ (NIST PQC + QKD) |
| **Political Acceptance** | ✓ (Western markets) | ✓ (Asian markets) |
| **NAT Traversal** | libp2p STUN/Relay | libp2p STUN/Relay |
| **Decentralization** | ✓ (DHT-based relays) | ✓ (DHT-based relays) |

---

## 🚀 Quick Start Commands

### Nokia Operator (Germany)
```bash
# 1. Install KIRQ Hub
curl -L https://get.quantumharmony.io/kirq-hub | bash

# 2. Connect your quantum devices (USB auto-detect)
kirq-hub scan

# 3. Start validator
quantumharmony-node \
  --validator \
  --chain nokia-mainnet \
  --nokia \
  --name "Deutsche-Telekom-Node-1"
```

### Asia Operator (Singapore)
```bash
# 1. Install KIRQ Hub with QKD
curl -L https://get.quantumharmony.asia/kirq-hub-qkd | bash

# 2. Configure QKD endpoints
kirq-hub config --qkd-peer validator-2.sg:3001

# 3. Start validator
quantumharmony-node \
  --validator \
  --chain asia-mainnet \
  --enable-qkd \
  --qkd-transport \
  --name "SingTel-QKD-Node-1"
```

---

## 📊 Monitoring Dashboard

```
┌─────────────────────────────────────────────────────────────┐
│  QuantumHarmony Validator Dashboard                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Device Health:                Network Status:              │
│  ✓ Toshiba    QBER: 0.9%      ✓ Peers: 47/50               │
│  ✓ Crypto4A   QBER: 0.5%      ✓ NAT: Direct (STUN)         │
│  ✓ IdQuantique QBER: 1.1%     ✓ Relay: 0ms latency         │
│                                                              │
│  Entropy Stats (Last 24h):    Consensus Participation:      │
│  • Collections: 1,440          • Leader: 287 times (19.9%)  │
│  • Avg size: 30 bytes          • Votes cast: 1,440 (100%)   │
│  • Failures: 0                 • Agreements: 1,437 (99.8%)  │
│                                                              │
│  Mode: NOKIA (Pure PQC) 🇩🇪    Uptime: 99.97% (364.9 days)  │
└─────────────────────────────────────────────────────────────┘
```
